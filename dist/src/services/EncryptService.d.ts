export default class EncryptService {
    private secret;
    constructor(secret: string);
    encrypt(text: string): string;
    decrypt(ciphertext: string): string;
}
//# sourceMappingURL=EncryptService.d.ts.map